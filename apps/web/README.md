# @aurafarming/web

Placeholder de workspace. O app Next.js 15 (App Router) real entra no
**Prompt 8** — aqui existe só o necessário para o pacote ser um membro válido
do workspace pnpm e provar, via [`tests/shared-import.smoke.test.ts`](./tests/shared-import.smoke.test.ts),
que `@aurafarming/shared` é importável a partir de `apps/web`.
